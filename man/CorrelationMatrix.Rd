% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s7_classes.R
\name{CorrelationMatrix}
\alias{CorrelationMatrix}
\title{CorrelationMatrix S7 Class}
\usage{
CorrelationMatrix(
  matrix = matrix(numeric(), nrow = 0, ncol = 0),
  n_hypotheses = 0L,
  n_analyses = 0L,
  column_names = character()
)
}
\description{
An S7 class for representing correlation matrices used in group sequential
and graph-based multiple testing procedures. Provides type-safe storage and
validation for correlation matrices with proper mathematical constraints.
}
\section{Fields}{

\describe{
\item{\code{matrix}}{A numeric matrix representing the correlation matrix}

\item{\code{n_hypotheses}}{Integer number of hypotheses}

\item{\code{n_analyses}}{Integer number of analyses}

\item{\code{column_names}}{Character vector of column names (e.g., "H1_A1", "H2_A1")}
}}

\examples{
library(tibble)

# Create sample event data
event_data <- tibble(
  H1 = c(1, 2, 1, 1, 2, 1),
  H2 = c(1, 2, 2, 1, 2, 2),
  Analysis = c(1, 1, 1, 2, 2, 2),
  Event = c(155, 160, 85, 305, 320, 170)
)

# Generate correlation matrix using traditional function
corr_matrix <- generate_corr(event_data)

# Create CorrelationMatrix object
corr_obj <- new_correlation_matrix(
  matrix = corr_matrix,
  n_hypotheses = 2,
  n_analyses = 2,
  column_names = colnames(corr_matrix)
)

print(corr_obj)

}
